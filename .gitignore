# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# Note: results/ 与 external/ 按项目约定纳入版本控制，故不在此忽略。

# build artifacts
*_submission.zip

# local scratch workspaces that are not part of the public release
mpl_error_curves/
archive/legacy_docs/kdroprelax_formula.md

# latex build artifacts (keep .tex and .pdf; drop intermediates)
paper/*.aux
paper/*.out
paper/*.log
slides/*.aux
slides/*.out
slides/*.nav
slides/*.snm
slides/*.toc
slides/*.vrb
slides/*.log

# represent/ independent reproduction: keep the evidence (reports, results,
# figs, scripts) but exclude bulky training data and the nested MPL clone.
represent/data/
represent/MultiPowerLaw/
represent/.DS_Store

# Local LaTeX compiler binary (50MB+, machine-specific)
tools/

# large regenerable trunk checkpoints (bitwise-reproducible via seed + G3 replay)
*.pt
# SSH helper holds the server password -- never commit
tools/srv.py
