# 演示稿(Beamer)

- 编译:Overleaf 选 pdfLaTeX,或本地 `latexmk -pdf main.tex`(需 beamer 宏包)。
- 提交前把作者行/封面里的 GitHub 链接 `wjjpku` 改成你的真实仓库。
- `main.pdf` 为已编译成品,可直接看/直接交。
- 13 帧、自包含:目标 → 背景/复现 → 推导 → 尺度不变性+塌缩 → 方法开发 → γ/edge-of-stability → 结论/复现。
